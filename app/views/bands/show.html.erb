<%- model_class = Band -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:title) %>:</strong></dt>
  <dd><%= @band.title %></dd>
  <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
  <dd><%= @band.description %></dd>
  <dt><strong><%= model_class.human_attribute_name(:active) %>:</strong></dt>
  <dd><%= @band.active %></dd>
  <dt><strong>Band members:</strong></dt>
  <dd>
    <% @band.band_roles.each do |band_member| %>
      <%= band_member.user.username %> <em>(<%= band_member.role %>)</em><br />
    <% end %>
  </dd>
</dl>

TODO display albums and songs <br /><br />

TODO display concerts <br /><br />

TODO display comments <br /><br />

<%= link_to t('.back', :default => t("helpers.links.back")),
              bands_path, :class => 'btn btn-default'  %>
<% if can? :update, @band %>
  <%= link_to 'Edit band',
                edit_band_path(@band), :class => 'btn btn-default' %>
<% end %>
<% if can? :destroy, @band %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                band_path(@band),
                :method => 'delete',
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'btn btn-danger' %>
<% end %>
