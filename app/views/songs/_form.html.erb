<%= form_for [@song.band, @song], :html => { :class => "form-horizontal song" } do |f| %>

  <% if @song.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @song.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%=f.error_span(:title) %>
  </div>
  <div class="form-group">
    <%= f.label :lyrics, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :lyrics, :class => 'form-control' %>
    </div>
    <%=f.error_span(:lyrics) %>
  </div>
  <div class="form-group">
    <%= f.label :composer, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :composer, :class => 'form-control' %>
    </div>
    <%=f.error_span(:composer) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                band_songs_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
