<%- model_class = Album -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= form_for band_albums_path(@band, @album) do |f| %>
  <%= render :partial => 'form', locals: {f: f} %>
<% end %>
