<p id="notice"><%= notice %></p>

<h1>Listing Songs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Lyrics</th>
      <th>Composer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.title %></td>
        <td><%= song.lyrics %></td>
        <td><%= song.composer %></td>
        <td><%= link_to 'Show', [song.band, song] %></td>
        <td><%= link_to 'Edit', edit_band_song_path(song.band, song) %></td>
        <td><%= link_to 'Destroy', [song.band, song], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Song', new_band_song_path %>
